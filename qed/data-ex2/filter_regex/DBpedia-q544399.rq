   PREFIX dbpedia2: <http://dbpedia.org/property/>   PREFIX foaf: <http://xmlns.com/foaf/0.1/>   PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>      SELECT DISTINCT ?artistName ?albumTitle           ?wpURL ?releaseDate ?coverURL   WHERE {     ?s dbpedia2:artist   ?artist;        dbpedia2:name     ?albumTitle;        foaf:page         ?wpURL;        dbpedia2:released ?releaseDate;        dbpedia2:cover    ?coverURL.     ?artist rdfs:label   ?artistName.      FILTER (regex(?artistName, '')).     FILTER (regex(?albumTitle, '')).     FILTER (lang(?artistName) = 'en').   }   LIMIT 30   