 PREFIX schema: <http://schema.org/> PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#> PREFIX wdt: <http://www.wikidata.org/prop/direct/> PREFIX wd: <http://www.wikidata.org/entity/>   SELECT ?director ?director_label (count(distinct ?film) as ?films) (count(distinct ?sitelink) as ?sitelinks) ((?films * ?sitelinks) as ?rank) WHERE { 	?director wdt:P106 wd:Q2526255 .  				 	?film wdt:P57 ?director . 	 					     ?sitelink schema:about ?director .				  	OPTIONAL {?director rdfs:label ?director_label                 FILTER (lang(?director_label) = "en")  	} 	 }  GROUP BY ?director ?director_label ORDER BY DESC(?rank) LIMIT 100 