BASE    <file:///Users/Veronika/Desktop/git/qed/qed-core/>
PREFIX  :     <http://dbpedia.org/resource/>
PREFIX  umbel-sc: <http://umbel.org/umbel/sc/>
PREFIX  dbpedia-owl: <http://dbpedia.org/ontology/>
PREFIX  owl:  <http://www.w3.org/2002/07/owl#>
PREFIX  yago: <http://dbpedia.org/class/yago/>
PREFIX  xsd:  <http://www.w3.org/2001/XMLSchema#>
PREFIX  skos: <http://www.w3.org/2004/02/skos/core#>
PREFIX  rdfs: <http://www.w3.org/2000/01/rdf-schema#>
PREFIX  dbpedia: <http://dbpedia.org/>
PREFIX  dbpedia-prop: <http://dbpedia.org/property/>
PREFIX  geo:  <http://www.w3.org/2003/01/geo/wgs84_pos#>
PREFIX  rdf:  <http://www.w3.org/1999/02/22-rdf-syntax-ns#>
PREFIX  foaf: <http://xmlns.com/foaf/0.1/>
PREFIX  dc:   <http://purl.org/dc/elements/1.1/>

CONSTRUCT 
  { 
    :Persistent_vegetative_state dbpedia-prop:redirect ?cond .
    ?cond rdfs:label "persistent vegetative state"@en .
    ?cond rdfs:label "persistent vegetative state (medical disorder)"@en .
    ?cond rdfs:label "persistent vegetative state (disease)"@en .
    ?cond dbpedia-prop:name "persistent vegetative state"@en .
    ?cond foaf:name "persistent vegetative state" .
    ?cond rdfs:label "Persistent vegetative state"@en .
    ?cond rdfs:label "Persistent vegetative state (medical disorder)"@en .
    ?cond rdfs:label "Persistent vegetative state (disease)"@en .
    ?cond foaf:name "Persistent vegetative state" .
    ?cond rdf:type dbpedia-owl:Disease .
    ?cond rdf:type ?condType .
    ?cond skos:subject ?subject .
    ?cond dbpedia-prop:wikiPageUsesTemplate ?template .
    ?cond owl:sameAs ?sameAsUri .
    ?cond foaf:page ?linkUrl .
  }
WHERE
  { { { FILTER NOT EXISTS { ?cond  owl:sameAs  ?sameAsUri }
        FILTER NOT EXISTS { ?cond  foaf:page  ?linkUrl }
      }
    }
    { FILTER NOT EXISTS { ?cond  rdf:type  dbpedia-owl:Disease }
      FILTER NOT EXISTS { ?cond  rdf:type  ?condType
                          FILTER regex(?condType, "http://dbpedia.org/class/yago/.*((Disease)|(Disorder)|(Symptoms)).*", "i")
                        }
      FILTER NOT EXISTS { ?cond  skos:subject  ?subject
                          FILTER regex(?subject, "http://dbpedia.org/resource/Category:.*((diseases)|(disorders)|(pain)|(abuse)|(conditions)|(symptoms)|(pathology)).*", "i")
                        }
      FILTER NOT EXISTS { ?cond  dbpedia-prop:wikiPageUsesTemplate  ?template
                          FILTER regex(?template, "http://dbpedia.org/resource/Template:((infobox_disease)|(diseasedisorder_infobox))", "i")
                        }
    }
    { FILTER NOT EXISTS { :Persistent_vegetative_state
                                    dbpedia-prop:redirect  ?cond
                        }
      FILTER NOT EXISTS { ?cond  rdfs:label  "persistent vegetative state"@en }
      FILTER NOT EXISTS { ?cond  rdfs:label  "persistent vegetative state (medical disorder)"@en }
      FILTER NOT EXISTS { ?cond  rdfs:label  "persistent vegetative state (disease)"@en }
      FILTER NOT EXISTS { ?cond  dbpedia-prop:name  "persistent vegetative state"@en }
      FILTER NOT EXISTS { ?cond  foaf:name  "persistent vegetative state" }
      FILTER NOT EXISTS { ?cond  rdfs:label  "Persistent vegetative state"@en }
      FILTER NOT EXISTS { ?cond  rdfs:label  "Persistent vegetative state (medical disorder)"@en }
      FILTER NOT EXISTS { ?cond  rdfs:label  "Persistent vegetative state (disease)"@en }
      FILTER NOT EXISTS { ?cond  foaf:name  "Persistent vegetative state" }
    }
  }
LIMIT   3

----------------------------------------------
BASE    <file:///Users/Veronika/Desktop/git/qed/qed-core/>
PREFIX  :     <http://dbpedia.org/resource/>
PREFIX  umbel-sc: <http://umbel.org/umbel/sc/>
PREFIX  dbpedia-owl: <http://dbpedia.org/ontology/>
PREFIX  owl:  <http://www.w3.org/2002/07/owl#>
PREFIX  yago: <http://dbpedia.org/class/yago/>
PREFIX  xsd:  <http://www.w3.org/2001/XMLSchema#>
PREFIX  skos: <http://www.w3.org/2004/02/skos/core#>
PREFIX  rdfs: <http://www.w3.org/2000/01/rdf-schema#>
PREFIX  dbpedia: <http://dbpedia.org/>
PREFIX  dbpedia-prop: <http://dbpedia.org/property/>
PREFIX  geo:  <http://www.w3.org/2003/01/geo/wgs84_pos#>
PREFIX  rdf:  <http://www.w3.org/1999/02/22-rdf-syntax-ns#>
PREFIX  foaf: <http://xmlns.com/foaf/0.1/>
PREFIX  dc:   <http://purl.org/dc/elements/1.1/>

CONSTRUCT 
  { 
    :Persistent_vegetative_state dbpedia-prop:redirect ?cond .
    ?cond rdfs:label "persistent vegetative state"@en .
    ?cond rdfs:label "persistent vegetative state (medical disorder)"@en .
    ?cond rdfs:label "persistent vegetative state (disease)"@en .
    ?cond dbpedia-prop:name "persistent vegetative state"@en .
    ?cond foaf:name "persistent vegetative state" .
    ?cond rdfs:label "Persistent vegetative state"@en .
    ?cond rdfs:label "Persistent vegetative state (medical disorder)"@en .
    ?cond rdfs:label "Persistent vegetative state (disease)"@en .
    ?cond foaf:name "Persistent vegetative state" .
    ?cond rdf:type dbpedia-owl:Disease .
    ?cond rdf:type ?condType .
    ?cond skos:subject ?subject .
    ?cond dbpedia-prop:wikiPageUsesTemplate ?template .
    ?cond owl:sameAs ?sameAsUri .
    ?cond foaf:page ?linkUrl .
  }
WHERE
  { { { FILTER NOT EXISTS { ?cond  owl:sameAs  ?sameAsUri }
        { ?cond  foaf:page  ?linkUrl }
      }
    }
    { FILTER NOT EXISTS { ?cond  rdf:type  dbpedia-owl:Disease }
      FILTER NOT EXISTS { ?cond  rdf:type  ?condType
                          FILTER regex(?condType, "http://dbpedia.org/class/yago/.*((Disease)|(Disorder)|(Symptoms)).*", "i")
                        }
      FILTER NOT EXISTS { ?cond  skos:subject  ?subject
                          FILTER regex(?subject, "http://dbpedia.org/resource/Category:.*((diseases)|(disorders)|(pain)|(abuse)|(conditions)|(symptoms)|(pathology)).*", "i")
                        }
      FILTER NOT EXISTS { ?cond  dbpedia-prop:wikiPageUsesTemplate  ?template
                          FILTER regex(?template, "http://dbpedia.org/resource/Template:((infobox_disease)|(diseasedisorder_infobox))", "i")
                        }
    }
    { FILTER NOT EXISTS { :Persistent_vegetative_state
                                    dbpedia-prop:redirect  ?cond
                        }
      FILTER NOT EXISTS { ?cond  rdfs:label  "persistent vegetative state"@en }
      FILTER NOT EXISTS { ?cond  rdfs:label  "persistent vegetative state (medical disorder)"@en }
      FILTER NOT EXISTS { ?cond  rdfs:label  "persistent vegetative state (disease)"@en }
      FILTER NOT EXISTS { ?cond  dbpedia-prop:name  "persistent vegetative state"@en }
      FILTER NOT EXISTS { ?cond  foaf:name  "persistent vegetative state" }
      FILTER NOT EXISTS { ?cond  rdfs:label  "Persistent vegetative state"@en }
      FILTER NOT EXISTS { ?cond  rdfs:label  "Persistent vegetative state (medical disorder)"@en }
      FILTER NOT EXISTS { ?cond  rdfs:label  "Persistent vegetative state (disease)"@en }
      FILTER NOT EXISTS { ?cond  foaf:name  "Persistent vegetative state" }
    }
  }
LIMIT   3

----------------------------------------------
BASE    <file:///Users/Veronika/Desktop/git/qed/qed-core/>
PREFIX  :     <http://dbpedia.org/resource/>
PREFIX  umbel-sc: <http://umbel.org/umbel/sc/>
PREFIX  dbpedia-owl: <http://dbpedia.org/ontology/>
PREFIX  owl:  <http://www.w3.org/2002/07/owl#>
PREFIX  yago: <http://dbpedia.org/class/yago/>
PREFIX  xsd:  <http://www.w3.org/2001/XMLSchema#>
PREFIX  skos: <http://www.w3.org/2004/02/skos/core#>
PREFIX  rdfs: <http://www.w3.org/2000/01/rdf-schema#>
PREFIX  dbpedia: <http://dbpedia.org/>
PREFIX  dbpedia-prop: <http://dbpedia.org/property/>
PREFIX  geo:  <http://www.w3.org/2003/01/geo/wgs84_pos#>
PREFIX  rdf:  <http://www.w3.org/1999/02/22-rdf-syntax-ns#>
PREFIX  foaf: <http://xmlns.com/foaf/0.1/>
PREFIX  dc:   <http://purl.org/dc/elements/1.1/>

CONSTRUCT 
  { 
    :Persistent_vegetative_state dbpedia-prop:redirect ?cond .
    ?cond rdfs:label "persistent vegetative state"@en .
    ?cond rdfs:label "persistent vegetative state (medical disorder)"@en .
    ?cond rdfs:label "persistent vegetative state (disease)"@en .
    ?cond dbpedia-prop:name "persistent vegetative state"@en .
    ?cond foaf:name "persistent vegetative state" .
    ?cond rdfs:label "Persistent vegetative state"@en .
    ?cond rdfs:label "Persistent vegetative state (medical disorder)"@en .
    ?cond rdfs:label "Persistent vegetative state (disease)"@en .
    ?cond foaf:name "Persistent vegetative state" .
    ?cond rdf:type dbpedia-owl:Disease .
    ?cond rdf:type ?condType .
    ?cond skos:subject ?subject .
    ?cond dbpedia-prop:wikiPageUsesTemplate ?template .
    ?cond owl:sameAs ?sameAsUri .
    ?cond foaf:page ?linkUrl .
  }
WHERE
  { { { { ?cond  owl:sameAs  ?sameAsUri }
        FILTER NOT EXISTS { ?cond  foaf:page  ?linkUrl }
      }
    }
    { FILTER NOT EXISTS { ?cond  rdf:type  dbpedia-owl:Disease }
      FILTER NOT EXISTS { ?cond  rdf:type  ?condType
                          FILTER regex(?condType, "http://dbpedia.org/class/yago/.*((Disease)|(Disorder)|(Symptoms)).*", "i")
                        }
      FILTER NOT EXISTS { ?cond  skos:subject  ?subject
                          FILTER regex(?subject, "http://dbpedia.org/resource/Category:.*((diseases)|(disorders)|(pain)|(abuse)|(conditions)|(symptoms)|(pathology)).*", "i")
                        }
      FILTER NOT EXISTS { ?cond  dbpedia-prop:wikiPageUsesTemplate  ?template
                          FILTER regex(?template, "http://dbpedia.org/resource/Template:((infobox_disease)|(diseasedisorder_infobox))", "i")
                        }
    }
    { FILTER NOT EXISTS { :Persistent_vegetative_state
                                    dbpedia-prop:redirect  ?cond
                        }
      FILTER NOT EXISTS { ?cond  rdfs:label  "persistent vegetative state"@en }
      FILTER NOT EXISTS { ?cond  rdfs:label  "persistent vegetative state (medical disorder)"@en }
      FILTER NOT EXISTS { ?cond  rdfs:label  "persistent vegetative state (disease)"@en }
      FILTER NOT EXISTS { ?cond  dbpedia-prop:name  "persistent vegetative state"@en }
      FILTER NOT EXISTS { ?cond  foaf:name  "persistent vegetative state" }
      FILTER NOT EXISTS { ?cond  rdfs:label  "Persistent vegetative state"@en }
      FILTER NOT EXISTS { ?cond  rdfs:label  "Persistent vegetative state (medical disorder)"@en }
      FILTER NOT EXISTS { ?cond  rdfs:label  "Persistent vegetative state (disease)"@en }
      FILTER NOT EXISTS { ?cond  foaf:name  "Persistent vegetative state" }
    }
  }
LIMIT   3

----------------------------------------------
BASE    <file:///Users/Veronika/Desktop/git/qed/qed-core/>
PREFIX  :     <http://dbpedia.org/resource/>
PREFIX  umbel-sc: <http://umbel.org/umbel/sc/>
PREFIX  dbpedia-owl: <http://dbpedia.org/ontology/>
PREFIX  owl:  <http://www.w3.org/2002/07/owl#>
PREFIX  yago: <http://dbpedia.org/class/yago/>
PREFIX  xsd:  <http://www.w3.org/2001/XMLSchema#>
PREFIX  skos: <http://www.w3.org/2004/02/skos/core#>
PREFIX  rdfs: <http://www.w3.org/2000/01/rdf-schema#>
PREFIX  dbpedia: <http://dbpedia.org/>
PREFIX  dbpedia-prop: <http://dbpedia.org/property/>
PREFIX  geo:  <http://www.w3.org/2003/01/geo/wgs84_pos#>
PREFIX  rdf:  <http://www.w3.org/1999/02/22-rdf-syntax-ns#>
PREFIX  foaf: <http://xmlns.com/foaf/0.1/>
PREFIX  dc:   <http://purl.org/dc/elements/1.1/>

CONSTRUCT 
  { 
    :Persistent_vegetative_state dbpedia-prop:redirect ?cond .
    ?cond rdfs:label "persistent vegetative state"@en .
    ?cond rdfs:label "persistent vegetative state (medical disorder)"@en .
    ?cond rdfs:label "persistent vegetative state (disease)"@en .
    ?cond dbpedia-prop:name "persistent vegetative state"@en .
    ?cond foaf:name "persistent vegetative state" .
    ?cond rdfs:label "Persistent vegetative state"@en .
    ?cond rdfs:label "Persistent vegetative state (medical disorder)"@en .
    ?cond rdfs:label "Persistent vegetative state (disease)"@en .
    ?cond foaf:name "Persistent vegetative state" .
    ?cond rdf:type dbpedia-owl:Disease .
    ?cond rdf:type ?condType .
    ?cond skos:subject ?subject .
    ?cond dbpedia-prop:wikiPageUsesTemplate ?template .
    ?cond owl:sameAs ?sameAsUri .
    ?cond foaf:page ?linkUrl .
  }
WHERE
  { { { { ?cond  owl:sameAs  ?sameAsUri }
        { ?cond  foaf:page  ?linkUrl }
      }
    }
    { FILTER NOT EXISTS { ?cond  rdf:type  dbpedia-owl:Disease }
      FILTER NOT EXISTS { ?cond  rdf:type  ?condType
                          FILTER regex(?condType, "http://dbpedia.org/class/yago/.*((Disease)|(Disorder)|(Symptoms)).*", "i")
                        }
      FILTER NOT EXISTS { ?cond  skos:subject  ?subject
                          FILTER regex(?subject, "http://dbpedia.org/resource/Category:.*((diseases)|(disorders)|(pain)|(abuse)|(conditions)|(symptoms)|(pathology)).*", "i")
                        }
      FILTER NOT EXISTS { ?cond  dbpedia-prop:wikiPageUsesTemplate  ?template
                          FILTER regex(?template, "http://dbpedia.org/resource/Template:((infobox_disease)|(diseasedisorder_infobox))", "i")
                        }
    }
    { FILTER NOT EXISTS { :Persistent_vegetative_state
                                    dbpedia-prop:redirect  ?cond
                        }
      FILTER NOT EXISTS { ?cond  rdfs:label  "persistent vegetative state"@en }
      FILTER NOT EXISTS { ?cond  rdfs:label  "persistent vegetative state (medical disorder)"@en }
      FILTER NOT EXISTS { ?cond  rdfs:label  "persistent vegetative state (disease)"@en }
      FILTER NOT EXISTS { ?cond  dbpedia-prop:name  "persistent vegetative state"@en }
      FILTER NOT EXISTS { ?cond  foaf:name  "persistent vegetative state" }
      FILTER NOT EXISTS { ?cond  rdfs:label  "Persistent vegetative state"@en }
      FILTER NOT EXISTS { ?cond  rdfs:label  "Persistent vegetative state (medical disorder)"@en }
      FILTER NOT EXISTS { ?cond  rdfs:label  "Persistent vegetative state (disease)"@en }
      FILTER NOT EXISTS { ?cond  foaf:name  "Persistent vegetative state" }
    }
  }
LIMIT   3

----------------------------------------------
BASE    <file:///Users/Veronika/Desktop/git/qed/qed-core/>
PREFIX  :     <http://dbpedia.org/resource/>
PREFIX  umbel-sc: <http://umbel.org/umbel/sc/>
PREFIX  dbpedia-owl: <http://dbpedia.org/ontology/>
PREFIX  owl:  <http://www.w3.org/2002/07/owl#>
PREFIX  yago: <http://dbpedia.org/class/yago/>
PREFIX  xsd:  <http://www.w3.org/2001/XMLSchema#>
PREFIX  skos: <http://www.w3.org/2004/02/skos/core#>
PREFIX  rdfs: <http://www.w3.org/2000/01/rdf-schema#>
PREFIX  dbpedia: <http://dbpedia.org/>
PREFIX  dbpedia-prop: <http://dbpedia.org/property/>
PREFIX  geo:  <http://www.w3.org/2003/01/geo/wgs84_pos#>
PREFIX  rdf:  <http://www.w3.org/1999/02/22-rdf-syntax-ns#>
PREFIX  foaf: <http://xmlns.com/foaf/0.1/>
PREFIX  dc:   <http://purl.org/dc/elements/1.1/>

CONSTRUCT 
  { 
    :Persistent_vegetative_state dbpedia-prop:redirect ?cond .
    ?cond rdfs:label "persistent vegetative state"@en .
    ?cond rdfs:label "persistent vegetative state (medical disorder)"@en .
    ?cond rdfs:label "persistent vegetative state (disease)"@en .
    ?cond dbpedia-prop:name "persistent vegetative state"@en .
    ?cond foaf:name "persistent vegetative state" .
    ?cond rdfs:label "Persistent vegetative state"@en .
    ?cond rdfs:label "Persistent vegetative state (medical disorder)"@en .
    ?cond rdfs:label "Persistent vegetative state (disease)"@en .
    ?cond foaf:name "Persistent vegetative state" .
    ?cond rdf:type dbpedia-owl:Disease .
    ?cond rdf:type ?condType .
    ?cond skos:subject ?subject .
    ?cond dbpedia-prop:wikiPageUsesTemplate ?template .
    ?cond owl:sameAs ?sameAsUri .
    ?cond foaf:page ?linkUrl .
  }
WHERE
  { FILTER NOT EXISTS {   { ?cond  owl:sameAs  ?sameAsUri }
                        UNION
                          { ?cond  foaf:page  ?linkUrl }
                      }
    { FILTER NOT EXISTS { ?cond  rdf:type  dbpedia-owl:Disease }
      FILTER NOT EXISTS { ?cond  rdf:type  ?condType
                          FILTER regex(?condType, "http://dbpedia.org/class/yago/.*((Disease)|(Disorder)|(Symptoms)).*", "i")
                        }
      FILTER NOT EXISTS { ?cond  skos:subject  ?subject
                          FILTER regex(?subject, "http://dbpedia.org/resource/Category:.*((diseases)|(disorders)|(pain)|(abuse)|(conditions)|(symptoms)|(pathology)).*", "i")
                        }
      FILTER NOT EXISTS { ?cond  dbpedia-prop:wikiPageUsesTemplate  ?template
                          FILTER regex(?template, "http://dbpedia.org/resource/Template:((infobox_disease)|(diseasedisorder_infobox))", "i")
                        }
    }
    { FILTER NOT EXISTS { :Persistent_vegetative_state
                                    dbpedia-prop:redirect  ?cond
                        }
      FILTER NOT EXISTS { ?cond  rdfs:label  "persistent vegetative state"@en }
      FILTER NOT EXISTS { ?cond  rdfs:label  "persistent vegetative state (medical disorder)"@en }
      FILTER NOT EXISTS { ?cond  rdfs:label  "persistent vegetative state (disease)"@en }
      FILTER NOT EXISTS { ?cond  dbpedia-prop:name  "persistent vegetative state"@en }
      FILTER NOT EXISTS { ?cond  foaf:name  "persistent vegetative state" }
      FILTER NOT EXISTS { ?cond  rdfs:label  "Persistent vegetative state"@en }
      FILTER NOT EXISTS { ?cond  rdfs:label  "Persistent vegetative state (medical disorder)"@en }
      FILTER NOT EXISTS { ?cond  rdfs:label  "Persistent vegetative state (disease)"@en }
      FILTER NOT EXISTS { ?cond  foaf:name  "Persistent vegetative state" }
    }
  }
LIMIT   3

----------------------------------------------
BASE    <file:///Users/Veronika/Desktop/git/qed/qed-core/>
PREFIX  :     <http://dbpedia.org/resource/>
PREFIX  umbel-sc: <http://umbel.org/umbel/sc/>
PREFIX  dbpedia-owl: <http://dbpedia.org/ontology/>
PREFIX  owl:  <http://www.w3.org/2002/07/owl#>
PREFIX  yago: <http://dbpedia.org/class/yago/>
PREFIX  xsd:  <http://www.w3.org/2001/XMLSchema#>
PREFIX  skos: <http://www.w3.org/2004/02/skos/core#>
PREFIX  rdfs: <http://www.w3.org/2000/01/rdf-schema#>
PREFIX  dbpedia: <http://dbpedia.org/>
PREFIX  dbpedia-prop: <http://dbpedia.org/property/>
PREFIX  geo:  <http://www.w3.org/2003/01/geo/wgs84_pos#>
PREFIX  rdf:  <http://www.w3.org/1999/02/22-rdf-syntax-ns#>
PREFIX  foaf: <http://xmlns.com/foaf/0.1/>
PREFIX  dc:   <http://purl.org/dc/elements/1.1/>

CONSTRUCT 
  { 
    :Persistent_vegetative_state dbpedia-prop:redirect ?cond .
    ?cond rdfs:label "persistent vegetative state"@en .
    ?cond rdfs:label "persistent vegetative state (medical disorder)"@en .
    ?cond rdfs:label "persistent vegetative state (disease)"@en .
    ?cond dbpedia-prop:name "persistent vegetative state"@en .
    ?cond foaf:name "persistent vegetative state" .
    ?cond rdfs:label "Persistent vegetative state"@en .
    ?cond rdfs:label "Persistent vegetative state (medical disorder)"@en .
    ?cond rdfs:label "Persistent vegetative state (disease)"@en .
    ?cond foaf:name "Persistent vegetative state" .
    ?cond rdf:type dbpedia-owl:Disease .
    ?cond rdf:type ?condType .
    ?cond skos:subject ?subject .
    ?cond dbpedia-prop:wikiPageUsesTemplate ?template .
    ?cond owl:sameAs ?sameAsUri .
    ?cond foaf:page ?linkUrl .
  }
WHERE
  { { { FILTER NOT EXISTS { ?cond  owl:sameAs  ?sameAsUri }
        FILTER NOT EXISTS { ?cond  foaf:page  ?linkUrl }
      }
    }
    { { ?cond  rdf:type  dbpedia-owl:Disease }
      FILTER NOT EXISTS { ?cond  rdf:type  ?condType
                          FILTER regex(?condType, "http://dbpedia.org/class/yago/.*((Disease)|(Disorder)|(Symptoms)).*", "i")
                        }
      FILTER NOT EXISTS { ?cond  skos:subject  ?subject
                          FILTER regex(?subject, "http://dbpedia.org/resource/Category:.*((diseases)|(disorders)|(pain)|(abuse)|(conditions)|(symptoms)|(pathology)).*", "i")
                        }
      FILTER NOT EXISTS { ?cond  dbpedia-prop:wikiPageUsesTemplate  ?template
                          FILTER regex(?template, "http://dbpedia.org/resource/Template:((infobox_disease)|(diseasedisorder_infobox))", "i")
                        }
    }
    { FILTER NOT EXISTS { :Persistent_vegetative_state
                                    dbpedia-prop:redirect  ?cond
                        }
      FILTER NOT EXISTS { ?cond  rdfs:label  "persistent vegetative state"@en }
      FILTER NOT EXISTS { ?cond  rdfs:label  "persistent vegetative state (medical disorder)"@en }
      FILTER NOT EXISTS { ?cond  rdfs:label  "persistent vegetative state (disease)"@en }
      FILTER NOT EXISTS { ?cond  dbpedia-prop:name  "persistent vegetative state"@en }
      FILTER NOT EXISTS { ?cond  foaf:name  "persistent vegetative state" }
      FILTER NOT EXISTS { ?cond  rdfs:label  "Persistent vegetative state"@en }
      FILTER NOT EXISTS { ?cond  rdfs:label  "Persistent vegetative state (medical disorder)"@en }
      FILTER NOT EXISTS { ?cond  rdfs:label  "Persistent vegetative state (disease)"@en }
      FILTER NOT EXISTS { ?cond  foaf:name  "Persistent vegetative state" }
    }
  }
LIMIT   3

----------------------------------------------
BASE    <file:///Users/Veronika/Desktop/git/qed/qed-core/>
PREFIX  :     <http://dbpedia.org/resource/>
PREFIX  umbel-sc: <http://umbel.org/umbel/sc/>
PREFIX  dbpedia-owl: <http://dbpedia.org/ontology/>
PREFIX  owl:  <http://www.w3.org/2002/07/owl#>
PREFIX  yago: <http://dbpedia.org/class/yago/>
PREFIX  xsd:  <http://www.w3.org/2001/XMLSchema#>
PREFIX  skos: <http://www.w3.org/2004/02/skos/core#>
PREFIX  rdfs: <http://www.w3.org/2000/01/rdf-schema#>
PREFIX  dbpedia: <http://dbpedia.org/>
PREFIX  dbpedia-prop: <http://dbpedia.org/property/>
PREFIX  geo:  <http://www.w3.org/2003/01/geo/wgs84_pos#>
PREFIX  rdf:  <http://www.w3.org/1999/02/22-rdf-syntax-ns#>
PREFIX  foaf: <http://xmlns.com/foaf/0.1/>
PREFIX  dc:   <http://purl.org/dc/elements/1.1/>

CONSTRUCT 
  { 
    :Persistent_vegetative_state dbpedia-prop:redirect ?cond .
    ?cond rdfs:label "persistent vegetative state"@en .
    ?cond rdfs:label "persistent vegetative state (medical disorder)"@en .
    ?cond rdfs:label "persistent vegetative state (disease)"@en .
    ?cond dbpedia-prop:name "persistent vegetative state"@en .
    ?cond foaf:name "persistent vegetative state" .
    ?cond rdfs:label "Persistent vegetative state"@en .
    ?cond rdfs:label "Persistent vegetative state (medical disorder)"@en .
    ?cond rdfs:label "Persistent vegetative state (disease)"@en .
    ?cond foaf:name "Persistent vegetative state" .
    ?cond rdf:type dbpedia-owl:Disease .
    ?cond rdf:type ?condType .
    ?cond skos:subject ?subject .
    ?cond dbpedia-prop:wikiPageUsesTemplate ?template .
    ?cond owl:sameAs ?sameAsUri .
    ?cond foaf:page ?linkUrl .
  }
WHERE
  { { { FILTER NOT EXISTS { ?cond  owl:sameAs  ?sameAsUri }
        { ?cond  foaf:page  ?linkUrl }
      }
    }
    { { ?cond  rdf:type  dbpedia-owl:Disease }
      FILTER NOT EXISTS { ?cond  rdf:type  ?condType
                          FILTER regex(?condType, "http://dbpedia.org/class/yago/.*((Disease)|(Disorder)|(Symptoms)).*", "i")
                        }
      FILTER NOT EXISTS { ?cond  skos:subject  ?subject
                          FILTER regex(?subject, "http://dbpedia.org/resource/Category:.*((diseases)|(disorders)|(pain)|(abuse)|(conditions)|(symptoms)|(pathology)).*", "i")
                        }
      FILTER NOT EXISTS { ?cond  dbpedia-prop:wikiPageUsesTemplate  ?template
                          FILTER regex(?template, "http://dbpedia.org/resource/Template:((infobox_disease)|(diseasedisorder_infobox))", "i")
                        }
    }
    { FILTER NOT EXISTS { :Persistent_vegetative_state
                                    dbpedia-prop:redirect  ?cond
                        }
      FILTER NOT EXISTS { ?cond  rdfs:label  "persistent vegetative state"@en }
      FILTER NOT EXISTS { ?cond  rdfs:label  "persistent vegetative state (medical disorder)"@en }
      FILTER NOT EXISTS { ?cond  rdfs:label  "persistent vegetative state (disease)"@en }
      FILTER NOT EXISTS { ?cond  dbpedia-prop:name  "persistent vegetative state"@en }
      FILTER NOT EXISTS { ?cond  foaf:name  "persistent vegetative state" }
      FILTER NOT EXISTS { ?cond  rdfs:label  "Persistent vegetative state"@en }
      FILTER NOT EXISTS { ?cond  rdfs:label  "Persistent vegetative state (medical disorder)"@en }
      FILTER NOT EXISTS { ?cond  rdfs:label  "Persistent vegetative state (disease)"@en }
      FILTER NOT EXISTS { ?cond  foaf:name  "Persistent vegetative state" }
    }
  }
LIMIT   3

----------------------------------------------
BASE    <file:///Users/Veronika/Desktop/git/qed/qed-core/>
PREFIX  :     <http://dbpedia.org/resource/>
PREFIX  umbel-sc: <http://umbel.org/umbel/sc/>
PREFIX  dbpedia-owl: <http://dbpedia.org/ontology/>
PREFIX  owl:  <http://www.w3.org/2002/07/owl#>
PREFIX  yago: <http://dbpedia.org/class/yago/>
PREFIX  xsd:  <http://www.w3.org/2001/XMLSchema#>
PREFIX  skos: <http://www.w3.org/2004/02/skos/core#>
PREFIX  rdfs: <http://www.w3.org/2000/01/rdf-schema#>
PREFIX  dbpedia: <http://dbpedia.org/>
PREFIX  dbpedia-prop: <http://dbpedia.org/property/>
PREFIX  geo:  <http://www.w3.org/2003/01/geo/wgs84_pos#>
PREFIX  rdf:  <http://www.w3.org/1999/02/22-rdf-syntax-ns#>
PREFIX  foaf: <http://xmlns.com/foaf/0.1/>
PREFIX  dc:   <http://purl.org/dc/elements/1.1/>

CONSTRUCT 
  { 
    :Persistent_vegetative_state dbpedia-prop:redirect ?cond .
    ?cond rdfs:label "persistent vegetative state"@en .
    ?cond rdfs:label "persistent vegetative state (medical disorder)"@en .
    ?cond rdfs:label "persistent vegetative state (disease)"@en .
    ?cond dbpedia-prop:name "persistent vegetative state"@en .
    ?cond foaf:name "persistent vegetative state" .
    ?cond rdfs:label "Persistent vegetative state"@en .
    ?cond rdfs:label "Persistent vegetative state (medical disorder)"@en .
    ?cond rdfs:label "Persistent vegetative state (disease)"@en .
    ?cond foaf:name "Persistent vegetative state" .
    ?cond rdf:type dbpedia-owl:Disease .
    ?cond rdf:type ?condType .
    ?cond skos:subject ?subject .
    ?cond dbpedia-prop:wikiPageUsesTemplate ?template .
    ?cond owl:sameAs ?sameAsUri .
    ?cond foaf:page ?linkUrl .
  }
WHERE
  { { { { ?cond  owl:sameAs  ?sameAsUri }
        FILTER NOT EXISTS { ?cond  foaf:page  ?linkUrl }
      }
    }
    { { ?cond  rdf:type  dbpedia-owl:Disease }
      FILTER NOT EXISTS { ?cond  rdf:type  ?condType
                          FILTER regex(?condType, "http://dbpedia.org/class/yago/.*((Disease)|(Disorder)|(Symptoms)).*", "i")
                        }
      FILTER NOT EXISTS { ?cond  skos:subject  ?subject
                          FILTER regex(?subject, "http://dbpedia.org/resource/Category:.*((diseases)|(disorders)|(pain)|(abuse)|(conditions)|(symptoms)|(pathology)).*", "i")
                        }
      FILTER NOT EXISTS { ?cond  dbpedia-prop:wikiPageUsesTemplate  ?template
                          FILTER regex(?template, "http://dbpedia.org/resource/Template:((infobox_disease)|(diseasedisorder_infobox))", "i")
                        }
    }
    { FILTER NOT EXISTS { :Persistent_vegetative_state
                                    dbpedia-prop:redirect  ?cond
                        }
      FILTER NOT EXISTS { ?cond  rdfs:label  "persistent vegetative state"@en }
      FILTER NOT EXISTS { ?cond  rdfs:label  "persistent vegetative state (medical disorder)"@en }
      FILTER NOT EXISTS { ?cond  rdfs:label  "persistent vegetative state (disease)"@en }
      FILTER NOT EXISTS { ?cond  dbpedia-prop:name  "persistent vegetative state"@en }
      FILTER NOT EXISTS { ?cond  foaf:name  "persistent vegetative state" }
      FILTER NOT EXISTS { ?cond  rdfs:label  "Persistent vegetative state"@en }
      FILTER NOT EXISTS { ?cond  rdfs:label  "Persistent vegetative state (medical disorder)"@en }
      FILTER NOT EXISTS { ?cond  rdfs:label  "Persistent vegetative state (disease)"@en }
      FILTER NOT EXISTS { ?cond  foaf:name  "Persistent vegetative state" }
    }
  }
LIMIT   3

----------------------------------------------
BASE    <file:///Users/Veronika/Desktop/git/qed/qed-core/>
PREFIX  :     <http://dbpedia.org/resource/>
PREFIX  umbel-sc: <http://umbel.org/umbel/sc/>
PREFIX  dbpedia-owl: <http://dbpedia.org/ontology/>
PREFIX  owl:  <http://www.w3.org/2002/07/owl#>
PREFIX  yago: <http://dbpedia.org/class/yago/>
PREFIX  xsd:  <http://www.w3.org/2001/XMLSchema#>
PREFIX  skos: <http://www.w3.org/2004/02/skos/core#>
PREFIX  rdfs: <http://www.w3.org/2000/01/rdf-schema#>
PREFIX  dbpedia: <http://dbpedia.org/>
PREFIX  dbpedia-prop: <http://dbpedia.org/property/>
PREFIX  geo:  <http://www.w3.org/2003/01/geo/wgs84_pos#>
PREFIX  rdf:  <http://www.w3.org/1999/02/22-rdf-syntax-ns#>
PREFIX  foaf: <http://xmlns.com/foaf/0.1/>
PREFIX  dc:   <http://purl.org/dc/elements/1.1/>

CONSTRUCT 
  { 
    :Persistent_vegetative_state dbpedia-prop:redirect ?cond .
    ?cond rdfs:label "persistent vegetative state"@en .
    ?cond rdfs:label "persistent vegetative state (medical disorder)"@en .
    ?cond rdfs:label "persistent vegetative state (disease)"@en .
    ?cond dbpedia-prop:name "persistent vegetative state"@en .
    ?cond foaf:name "persistent vegetative state" .
    ?cond rdfs:label "Persistent vegetative state"@en .
    ?cond rdfs:label "Persistent vegetative state (medical disorder)"@en .
    ?cond rdfs:label "Persistent vegetative state (disease)"@en .
    ?cond foaf:name "Persistent vegetative state" .
    ?cond rdf:type dbpedia-owl:Disease .
    ?cond rdf:type ?condType .
    ?cond skos:subject ?subject .
    ?cond dbpedia-prop:wikiPageUsesTemplate ?template .
    ?cond owl:sameAs ?sameAsUri .
    ?cond foaf:page ?linkUrl .
  }
WHERE
  { { { { ?cond  owl:sameAs  ?sameAsUri }
        { ?cond  foaf:page  ?linkUrl }
      }
    }
    { { ?cond  rdf:type  dbpedia-owl:Disease }
      FILTER NOT EXISTS { ?cond  rdf:type  ?condType
                          FILTER regex(?condType, "http://dbpedia.org/class/yago/.*((Disease)|(Disorder)|(Symptoms)).*", "i")
                        }
      FILTER NOT EXISTS { ?cond  skos:subject  ?subject
                          FILTER regex(?subject, "http://dbpedia.org/resource/Category:.*((diseases)|(disorders)|(pain)|(abuse)|(conditions)|(symptoms)|(pathology)).*", "i")
                        }
      FILTER NOT EXISTS { ?cond  dbpedia-prop:wikiPageUsesTemplate  ?template
                          FILTER regex(?template, "http://dbpedia.org/resource/Template:((infobox_disease)|(diseasedisorder_infobox))", "i")
                        }
    }
    { FILTER NOT EXISTS { :Persistent_vegetative_state
                                    dbpedia-prop:redirect  ?cond
                        }
      FILTER NOT EXISTS { ?cond  rdfs:label  "persistent vegetative state"@en }
      FILTER NOT EXISTS { ?cond  rdfs:label  "persistent vegetative state (medical disorder)"@en }
      FILTER NOT EXISTS { ?cond  rdfs:label  "persistent vegetative state (disease)"@en }
      FILTER NOT EXISTS { ?cond  dbpedia-prop:name  "persistent vegetative state"@en }
      FILTER NOT EXISTS { ?cond  foaf:name  "persistent vegetative state" }
      FILTER NOT EXISTS { ?cond  rdfs:label  "Persistent vegetative state"@en }
      FILTER NOT EXISTS { ?cond  rdfs:label  "Persistent vegetative state (medical disorder)"@en }
      FILTER NOT EXISTS { ?cond  rdfs:label  "Persistent vegetative state (disease)"@en }
      FILTER NOT EXISTS { ?cond  foaf:name  "Persistent vegetative state" }
    }
  }
LIMIT   3

----------------------------------------------
BASE    <file:///Users/Veronika/Desktop/git/qed/qed-core/>
PREFIX  :     <http://dbpedia.org/resource/>
PREFIX  umbel-sc: <http://umbel.org/umbel/sc/>
PREFIX  dbpedia-owl: <http://dbpedia.org/ontology/>
PREFIX  owl:  <http://www.w3.org/2002/07/owl#>
PREFIX  yago: <http://dbpedia.org/class/yago/>
PREFIX  xsd:  <http://www.w3.org/2001/XMLSchema#>
PREFIX  skos: <http://www.w3.org/2004/02/skos/core#>
PREFIX  rdfs: <http://www.w3.org/2000/01/rdf-schema#>
PREFIX  dbpedia: <http://dbpedia.org/>
PREFIX  dbpedia-prop: <http://dbpedia.org/property/>
PREFIX  geo:  <http://www.w3.org/2003/01/geo/wgs84_pos#>
PREFIX  rdf:  <http://www.w3.org/1999/02/22-rdf-syntax-ns#>
PREFIX  foaf: <http://xmlns.com/foaf/0.1/>
PREFIX  dc:   <http://purl.org/dc/elements/1.1/>

CONSTRUCT 
  { 
    :Persistent_vegetative_state dbpedia-prop:redirect ?cond .
    ?cond rdfs:label "persistent vegetative state"@en .
    ?cond rdfs:label "persistent vegetative state (medical disorder)"@en .
    ?cond rdfs:label "persistent vegetative state (disease)"@en .
    ?cond dbpedia-prop:name "persistent vegetative state"@en .
    ?cond foaf:name "persistent vegetative state" .
    ?cond rdfs:label "Persistent vegetative state"@en .
    ?cond rdfs:label "Persistent vegetative state (medical disorder)"@en .
    ?cond rdfs:label "Persistent vegetative state (disease)"@en .
    ?cond foaf:name "Persistent vegetative state" .
    ?cond rdf:type dbpedia-owl:Disease .
    ?cond rdf:type ?condType .
    ?cond skos:subject ?subject .
    ?cond dbpedia-prop:wikiPageUsesTemplate ?template .
    ?cond owl:sameAs ?sameAsUri .
    ?cond foaf:page ?linkUrl .
  }
WHERE
  { FILTER NOT EXISTS {   { ?cond  owl:sameAs  ?sameAsUri }
                        UNION
                          { ?cond  foaf:page  ?linkUrl }
                      }
    { { ?cond  rdf:type  dbpedia-owl:Disease }
      FILTER NOT EXISTS { ?cond  rdf:type  ?condType
                          FILTER regex(?condType, "http://dbpedia.org/class/yago/.*((Disease)|(Disorder)|(Symptoms)).*", "i")
                        }
      FILTER NOT EXISTS { ?cond  skos:subject  ?subject
                          FILTER regex(?subject, "http://dbpedia.org/resource/Category:.*((diseases)|(disorders)|(pain)|(abuse)|(conditions)|(symptoms)|(pathology)).*", "i")
                        }
      FILTER NOT EXISTS { ?cond  dbpedia-prop:wikiPageUsesTemplate  ?template
                          FILTER regex(?template, "http://dbpedia.org/resource/Template:((infobox_disease)|(diseasedisorder_infobox))", "i")
                        }
    }
    { FILTER NOT EXISTS { :Persistent_vegetative_state
                                    dbpedia-prop:redirect  ?cond
                        }
      FILTER NOT EXISTS { ?cond  rdfs:label  "persistent vegetative state"@en }
      FILTER NOT EXISTS { ?cond  rdfs:label  "persistent vegetative state (medical disorder)"@en }
      FILTER NOT EXISTS { ?cond  rdfs:label  "persistent vegetative state (disease)"@en }
      FILTER NOT EXISTS { ?cond  dbpedia-prop:name  "persistent vegetative state"@en }
      FILTER NOT EXISTS { ?cond  foaf:name  "persistent vegetative state" }
      FILTER NOT EXISTS { ?cond  rdfs:label  "Persistent vegetative state"@en }
      FILTER NOT EXISTS { ?cond  rdfs:label  "Persistent vegetative state (medical disorder)"@en }
      FILTER NOT EXISTS { ?cond  rdfs:label  "Persistent vegetative state (disease)"@en }
      FILTER NOT EXISTS { ?cond  foaf:name  "Persistent vegetative state" }
    }
  }
LIMIT   3

----------------------------------------------
BASE    <file:///Users/Veronika/Desktop/git/qed/qed-core/>
PREFIX  :     <http://dbpedia.org/resource/>
PREFIX  umbel-sc: <http://umbel.org/umbel/sc/>
PREFIX  dbpedia-owl: <http://dbpedia.org/ontology/>
PREFIX  owl:  <http://www.w3.org/2002/07/owl#>
PREFIX  yago: <http://dbpedia.org/class/yago/>
PREFIX  xsd:  <http://www.w3.org/2001/XMLSchema#>
PREFIX  skos: <http://www.w3.org/2004/02/skos/core#>
PREFIX  rdfs: <http://www.w3.org/2000/01/rdf-schema#>
PREFIX  dbpedia: <http://dbpedia.org/>
PREFIX  dbpedia-prop: <http://dbpedia.org/property/>
PREFIX  geo:  <http://www.w3.org/2003/01/geo/wgs84_pos#>
PREFIX  rdf:  <http://www.w3.org/1999/02/22-rdf-syntax-ns#>
PREFIX  foaf: <http://xmlns.com/foaf/0.1/>
PREFIX  dc:   <http://purl.org/dc/elements/1.1/>

CONSTRUCT 
  { 
    :Persistent_vegetative_state dbpedia-prop:redirect ?cond .
    ?cond rdfs:label "persistent vegetative state"@en .
    ?cond rdfs:label "persistent vegetative state (medical disorder)"@en .
    ?cond rdfs:label "persistent vegetative state (disease)"@en .
    ?cond dbpedia-prop:name "persistent vegetative state"@en .
    ?cond foaf:name "persistent vegetative state" .
    ?cond rdfs:label "Persistent vegetative state"@en .
    ?cond rdfs:label "Persistent vegetative state (medical disorder)"@en .
    ?cond rdfs:label "Persistent vegetative state (disease)"@en .
    ?cond foaf:name "Persistent vegetative state" .
    ?cond rdf:type dbpedia-owl:Disease .
    ?cond rdf:type ?condType .
    ?cond skos:subject ?subject .
    ?cond dbpedia-prop:wikiPageUsesTemplate ?template .
    ?cond owl:sameAs ?sameAsUri .
    ?cond foaf:page ?linkUrl .
  }
WHERE
  { { { FILTER NOT EXISTS { ?cond  owl:sameAs  ?sameAsUri }
        FILTER NOT EXISTS { ?cond  foaf:page  ?linkUrl }
      }
    }
    { FILTER NOT EXISTS { ?cond  rdf:type  dbpedia-owl:Disease }
      FILTER NOT EXISTS { ?cond  rdf:type  ?condType
                          FILTER regex(?condType, "http://dbpedia.org/class/yago/.*((Disease)|(Disorder)|(Symptoms)).*", "i")
                        }
      FILTER NOT EXISTS { ?cond  skos:subject  ?subject
                          FILTER regex(?subject, "http://dbpedia.org/resource/Category:.*((diseases)|(disorders)|(pain)|(abuse)|(conditions)|(symptoms)|(pathology)).*", "i")
                        }
      FILTER NOT EXISTS { ?cond  dbpedia-prop:wikiPageUsesTemplate  ?template
                          FILTER regex(?template, "http://dbpedia.org/resource/Template:((infobox_disease)|(diseasedisorder_infobox))", "i")
                        }
    }
    { FILTER NOT EXISTS { :Persistent_vegetative_state
                                    dbpedia-prop:redirect  ?cond
                        }
      FILTER NOT EXISTS { ?cond  rdfs:label  "persistent vegetative state"@en }
      FILTER NOT EXISTS { ?cond  rdfs:label  "persistent vegetative state (medical disorder)"@en }
      FILTER NOT EXISTS { ?cond  rdfs:label  "persistent vegetative state (disease)"@en }
      FILTER NOT EXISTS { ?cond  dbpedia-prop:name  "persistent vegetative state"@en }
      FILTER NOT EXISTS { ?cond  foaf:name  "persistent vegetative state" }
      FILTER NOT EXISTS { ?cond  rdfs:label  "Persistent vegetative state"@en }
      FILTER NOT EXISTS { ?cond  rdfs:label  "Persistent vegetative state (medical disorder)"@en }
      FILTER NOT EXISTS { ?cond  rdfs:label  "Persistent vegetative state (disease)"@en }
      FILTER NOT EXISTS { ?cond  foaf:name  "Persistent vegetative state" }
    }
  }
LIMIT   3

----------------------------------------------
